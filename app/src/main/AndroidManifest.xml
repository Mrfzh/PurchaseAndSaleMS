<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.feng.purchaseandsalems">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".view.StockAlterActivity"></activity>
        <activity android:name=".view.StockInsertActivity" />
        <activity android:name=".view.StockActivity" />
        <activity android:name=".view.PurchaseAlterActivity" />
        <activity android:name=".view.StaffInsertActivity" />
        <activity android:name=".view.AutopartsInsertActivity" />
        <activity android:name=".view.PurchaseInsertActivity" />
        <activity android:name=".view.PurchaseActivity" />
        <activity android:name=".view.RegisterActivity" />
        <activity android:name=".view.LoginActivity" />
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>